{
  "name": "svelte-apps",
  "private": true,
  "version": "0.0.1",
  "license": "MIT",
  "scripts": {
    "build": "rollup -c rollup.config.js",
    "watch": "rollup -c rollup.config.js --watch"
  },
  "dependencies": {
    "svelte": "^3.6.1"
  },
  "devDependencies": {
    "rollup-plugin-svelte": "^5.1.0"
  }
}
