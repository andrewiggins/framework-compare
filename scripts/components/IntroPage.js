import { h, Fragment } from "preact";
import { PageHeader } from "./PageHeader";

export const IntroPage = props => (
	<Fragment>
		<PageHeader title="Introduction" />
		<div>TODO: Fill out</div>
	</Fragment>
);
